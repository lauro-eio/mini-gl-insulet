# Insulet (client) — not Global Leadership catalog

This folder holds a **tailored client workshop** for Insulet.

It is **outside** the Global Leadership factory scope (`library/`, `content/`, ATC and other catalog programs).

- Do not treat `INSULET` as a `program_id` in GL schemas.
- Do not publish GL/ATC HTML into the Insulet hosting repo.
- Do not list Insulet on the GL factory `index.html` catalog.
